# Architecture Decisions

No application architecture has been selected yet.

The primary Codex implementation thread will propose the smallest reliable architecture that can:

- deploy as a responsive web application;
- keep the OpenAI API key server-side;
- call GPT-5.6 meaningfully;
- support the fixed two-stage Moment Coach loop;
- save simple history locally in the browser;
- remain testable and demonstrable before the internal freeze.
